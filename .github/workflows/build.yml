name: Build And Push To Registry
on:
  push:
    branches:
      - release/mrsk
jobs:
  push_to_registry:
    name: Push Docker image
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: set ssh
        run: |
          mkdir -p ~/.ssh/
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_ed25519

      - name: set env file
        run: |
          echo "RAILS_MASTER_KEY= ${{ secrets.RAILS_MASTER_KEY }}" >> .env
          echo "MRSK_REGISTRY_PASSWORD= ${{ secrets.CR_PAT }}" >> .env

      - name: Login to GitHub Container Registry
        run: echo ${{ secrets.CR_PAT }} | docker login ghcr.io -u junkers21 --password-stdin

      - name: Set up Docker Buildx.
        uses: docker/setup-buildx-action@v2

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: ghcr.io/junkers21/simple_blog_ror:${{ github.sha }}, ghcr.io/junkers21/simple_blog_ror:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: MRSK deploy
        run: 'mrsk deploy -vvvv --skip_push'
